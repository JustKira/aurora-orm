[package]
name = "aurora-config"
description = "Shared `aurora.toml` config schema and loader for Aurora tools."
version = "0.0.0"
edition = "2024"
publish = false
license = "UNLICENSED"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
toml = "0.8"
