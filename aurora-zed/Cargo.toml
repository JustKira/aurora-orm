[package]
name = "aurora-zed"
description = "Zed editor extension for the Aurora schema language."
version = "0.1.0"
edition = "2021"
publish = false
license = "UNLICENSED"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.5"
